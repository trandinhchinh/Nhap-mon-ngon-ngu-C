#include <stdio.h>
int main(){
	int s, n, sum = 0;
	printf("Nhap so n:");
  	scanf("%d",&n);
	printf("n = %d\n",n);
	for (s=1; s <= n; s++){
		sum = sum+s*s;
	}
  if (n == 1)
  printf("s=1^2= %d", sum );
  else if (n == 2)
  printf("s=1^2 + 2^2 = %d", sum );
  else
  printf("Tong =  %d",sum);
}
